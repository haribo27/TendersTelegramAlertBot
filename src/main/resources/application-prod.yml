spring:
  datasource:
    url: jdbc:postgresql://tenderbot-postgres:5432/tender_alert_bot
    username: tenderbot
    password: 77777751

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 1
    baseline-description: "Baseline Migration"

  config:
    activate:
      on-profile: prod